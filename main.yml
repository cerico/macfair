---
- name: Before everything
  hosts: localhost
  roles:
    - { role: before, tags: [always] }

- name: Setup MacBook and debian
  hosts: [macbook, debian]
  roles:
    - { role: whoami, tags: [always] }
    - { role: install, tags: [install, all, info] }
    - { role: functions, tags: [functions, install, all, info] }
    - { role: terminal, tags: [terminal, all, info] }

- name: Setup debian machines
  hosts: [debian]
  roles:
    - { role: install, tags: [debian] }
    - { role: terminal, tags: [debian] }
    - { role: functions, tags: [debian] }
    - { role: nginx, tags: [debian, nginx] }

- name: Setup macbook
  hosts: macbook
  roles:
    - { role: vscode, tags: [vscode, all, info] }
    - { role: desktop, tags: [desktop, all, info] }
    - { role: keepalive, tags: [keepalive, functions, all, info] }
    - { role: rails, tags: [rails, all, info] }
    - { role: elixir, tags: [elixir, all, info] }

- name: After everything
  hosts: localhost
  roles:
    - { role: afterwards, tags: [always] }
